import React from 'react';
import { Entry } from '../entries';

export default function HabitEntry({ entry }: { entry: Entry }) {
{/*
  How do we want to display a habit entry?
  Habits have to be setup by the user, when a user selects the habit entry type, 
  they will be prompted to create a habit or choose one that's current.
  Each habit entry has the following properties:
  - A title
  - A description
  - A frequency
  - A progress bar
  - A button to mark the habit as complete
  - A button to edit the habit
*/}
  return <div>HabitEntry</div>;
}
