import React from 'react';
import { Entry } from '../entries';
export default function TaskEntry({ entry }: { entry: Entry }) {
  {
    /*
  How do we want to display a task entry?
  Each task entry has a title, 
  a content section which hold a checklist of tasks with thier own name, status, and due date, 
  a progress bar, 
  and a button to mark the task as complete or all as completed.
*/
  }
  return <div>TaskEntry</div>;
}
